import React from 'react';

const DocumentTracker = ({ role, documents }) => (
  <section>
    {/* Checklist and filters will go here */}
    <div>DocumentTracker for role: {role}</div>
  </section>
);

export default DocumentTracker; 