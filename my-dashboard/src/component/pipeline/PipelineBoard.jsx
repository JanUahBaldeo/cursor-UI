import React from 'react';

const PipelineBoard = ({ stages, cards, role }) => (
  <section>
    {/* Pipeline metrics, filters, and Kanban columns will go here */}
    <div>PipelineBoard for role: {role}</div>
  </section>
);

export default PipelineBoard; 